@{
    ViewBag.Title = "Acceso Denegado - InfinityGrowth";
    Layout = "~/Views/Shared/_Layout_Login.cshtml";
}

<section class="flex items-center justify-center gap-20 bg-[var(--bg-color)]">
    <img src="~/public/img/403-image.png" alt="Imagen 403 Acceso denegado" height="auto" width="600px" class="mb-10 rounded-2xl shadow-2xl"/>
    <div class="text-left">
        <h3 class="text-8xl font-teko font-bold text-red-500 mb-8">403</h3>
        <h3 class="text-4xl font-teko font-bold text-[var(--text-primary)] mb-4">Acceso Denegado</h3>
        <p class="text-xl text-[var(--text-primary)] mb-8">
            No tienes permisos para ver esta página.<br />
            Si crees que esto es un error, contacta con el administrador.
        </p>
        <a asp-controller="Home" asp-action="Index" class="btn-primary w-40">
            Volver al Inicio
        </a>
    </div>
</section>